[package]
name = "skaidb-proto"
description = "skaidb's binary wire protocol (SCP): request and response frames, value encoding and consistency levels shared by the server and its drivers"
version = "0.294.0"
edition = "2021"
license = "SSPL-1.0"
repository = "https://github.com/porcupin26/skaidb-rust"
rust-version = "1.93"

[lints]
workspace = true

[dependencies]
skaidb-types = { path = "../skaidb-types", version = "0.294.0" }
thiserror.workspace = true
