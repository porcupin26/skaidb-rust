[package]
name = "skaidb-auth"
description = "Authentication primitives for skaidb: SCRAM-SHA-256 exchange, SHA-256/HMAC/PBKDF2 and credential handling shared by the server and its drivers"
version = "0.294.0"
edition = "2021"
license = "SSPL-1.0"
repository = "https://github.com/porcupin26/skaidb-rust"
rust-version = "1.93"

[lints]
workspace = true

[dependencies]
thiserror.workspace = true
