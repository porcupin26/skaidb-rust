[package]
name = "skaidb-types"
description = "Core value and document model of skaidb — the schema-less row representation, key encoding and type rules shared by the server and its drivers"
version = "0.294.0"
edition = "2021"
license = "SSPL-1.0"
repository = "https://github.com/porcupin26/skaidb-rust"
rust-version = "1.93"

[lints]
workspace = true

[dependencies]
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
